# Database (binary, use backups instead)
data/*.db
data/*.db-wal
data/*.db-shm
data/backups/
data/server.log
data/prompts.log
data/worker.log

# Personal goal-keyword personalisation (names real people/places; never committed)
data/goal_keywords.local.json

# Claude Code settings (personal, not project-level)
.claude/settings.local.json

# OS files
.DS_Store
**/.DS_Store

# Environment (secrets)
.env

# Deploy config (real server domain/user, gitignored — copy from deploy.conf.example)
deploy.conf

# Python
__pycache__/
*.pyc

# Temp files
*.tmp
*.swp
