# lifeplan -- shared rate-limit zone for /lifeplan
# Dropped into /etc/nginx/conf.d/ so it's loaded in the http {} context
# before any site config references `zone=authzone`.
limit_req_zone $binary_remote_addr zone=authzone:10m rate=10r/s;
